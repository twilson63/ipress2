{
  "name": "svelte-app",
  "version": "1.0.0",
  "devDependencies": {
    "mdsvex": "^0.3.0",
    "npm-run-all": "^4.1.5",
    "rollup": "^1.10.1",
    "rollup-plugin-commonjs": "^9.3.4",
    "rollup-plugin-multi-input": "^1.0.0",
    "rollup-plugin-node-resolve": "^4.2.3",
    "rollup-plugin-svelte": "^5.0.3",
    "svelte": "^3.0.0",
    "w3": "^2.0.0"
  },
  "scripts": {
    "build": "run-s clean build:one build:two",
    "clean": "rm -rf dist",
    "build:one": "rollup -c",
    "build:two": "node build.js",
    "start": "npx w3 dist"
  },
  "dependencies": {
    "klaw-sync": "^6.0.0"
  }
}
